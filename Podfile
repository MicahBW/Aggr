# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Aggr' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Aggr
  pod 'GoogleMaps'
  pod 'GooglePlaces'

  target 'AggrTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'AggrUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
